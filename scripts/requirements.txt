# Deps for refresh-reviews.py (single-business Google review scraper).
# Same stealth stack as the agency's bulk Google Maps scraper. Install once:
#   python3 -m pip install -r scripts/requirements.txt
#   python3 -m patchright install chromium   # the browser StealthySession launches
# NOTE: scrapling's StealthySession drives patchright's stealth Chromium, so the
# Chromium binary (via `patchright install chromium`) is what's actually required —
# the camoufox pip package is pulled in as a dep but its browser binary is not needed.
# Tested against scrapling 0.4.9.
scrapling[fetchers]>=0.4
camoufox
